---
# Supported: 14, 15, 16
postgresql_version: 14

postgresql_init_cmd_options: "--encoding=UTF-8 --no-locale"

postgresql_rpm: "https://download.postgresql.org/pub/repos/yum/reporpms/EL-9-x86_64/pgdg-redhat-repo-latest.noarch.rpm"
